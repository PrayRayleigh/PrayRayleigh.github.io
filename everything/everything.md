# 杂记
  这里放一些乱七八糟的东西
  # Math and TeX block

$$f(x) = x * e^{2 pi i \xi x}$$
# code support
  ```cpp
  <?cpp
      #include "iostream"
      using namespace std;
      int main(){
        cout>>"helloworld">>endl;
        return 0;
      }
  ?>
  下次再做吧 